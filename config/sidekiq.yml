concurrency: 10
timeout: 25
retry: 1
queues:
  - default
  - clock
  - providers
  - billing
  - webhook
  - invoices
  - wallets

production:
  concurrency: <%= ENV('SIDEKIQ_CONCURRENCY').fetch { 10 } %>
staging:
  concurrency: 10
